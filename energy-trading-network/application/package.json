{
  "name": "energy-trading-app",
  "version": "1.0.0",
  "description": "Energy trading application with PostgreSQL backend (blockchain optional)",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "enroll": "node enrollAdmin.js",
    "register": "node registerFactory.js",
    "db:start": "docker-compose -f docker-compose.postgresql.yml up -d",
    "db:stop": "docker-compose -f docker-compose.postgresql.yml down"
  },
  "keywords": ["energy", "trading", "mobile", "postgresql", "factories"],
  "author": "Energy Trading Zone",
  "license": "Apache-2.0",
  "dependencies": {
    "express": "^4.18.2",
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.3.1",
    "pg": "^8.11.3"
  },
  "optionalDependencies": {
    "fabric-network": "^2.2.20",
    "fabric-ca-client": "^2.2.20"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
