# DEPRECATED - This directory is no longer used
# 
# The mobile app backend has been merged with the unified blockchain application.
# 
# CURRENT ARCHITECTURE:
# ======================
# 1. PostgreSQL - For credentials and non-essential data
#    Location: energy-trading-network/application/docker-compose.postgresql.yml
#    Start: cd energy-trading-network/application && npm run db:start
#
# 2. CouchDB (via Hyperledger Fabric) - For trading data on blockchain
#    Start: cd energy-trading-network/network && ./startNetwork.sh
#
# 3. Unified Application
#    Location: energy-trading-network/application/app.js
#    Start: cd energy-trading-network/application && npm install && npm start
#
# See: energy-trading-network/application/docker-compose.postgresql.yml

version: '3.8'

services:
  deprecated-notice:
    image: alpine:latest
    container_name: deprecated-backend-notice
    command: >
      echo "NOTICE: This docker-compose is deprecated.
      The mobile backend has been merged with the blockchain application.
      
      To start the application:
      1. Start PostgreSQL: cd energy-trading-network/application && npm run db:start
      2. Start blockchain: cd energy-trading-network/network && ./startNetwork.sh
      3. Start app: cd energy-trading-network/application && npm start
      
      Databases:
      - PostgreSQL: Credentials, profiles, login history
      - CouchDB: Trading data (energy, currency, trades)"
    restart: "no"

networks:
  energy-network:
    driver: bridge
