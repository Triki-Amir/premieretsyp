# NOTE: This docker-compose.yml is DEPRECATED
# The mobile app backend has been merged with the blockchain application.
# The unified application is located at: energy-trading-network/application/app.js
# 
# MySQL is NO LONGER USED - all data is stored in CouchDB via Hyperledger Fabric
# The CouchDB instances (couchdb0 and couchdb1) are started by the blockchain network.
#
# To start the application:
# 1. Start the blockchain network: cd energy-trading-network/network && ./startNetwork.sh
# 2. Deploy chaincode: ./deployChaincode.sh
# 3. Start the unified app: cd ../application && npm install && npm start
#
# This file is kept for reference only and should not be used.

services:
  # MySQL has been removed - using CouchDB via Hyperledger Fabric instead
  # The backend has been merged with the blockchain application
  
  app-info:
    image: alpine:latest
    container_name: deprecated-notice
    command: >
      echo "NOTICE: This docker-compose is deprecated. 
      The mobile backend has been merged with the blockchain application.
      Please use: energy-trading-network/application/app.js
      Database: CouchDB via Hyperledger Fabric (couchdb0, couchdb1)"
    restart: "no"

networks:
  energy-network:
    driver: bridge
